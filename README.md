# stoat-and-ferret

[Alpha] AI-driven video editor with hybrid Python/Rust architecture - not production ready.

**v075 (Release 2, Wave 0 — Enablers):** Added `compile_automation` Rust function with 4 curve kinds (linear, ease-in, ease-out, ease-in-out) and PyO3 binding (BL-418, PR #516); wired `AutomationEnvelope` union type into effect API with `filter_preview` compiled expression (BL-420, PR #517); added automation envelope smoke tests (PR #518); established Release 2 test scaffolding — `tests/qc/`, `tests/chatbot/`, `tests/uat/` skeleton (BL-421, PR #519); added project timeline markers CRUD REST API with Alembic migration and non-overlap validation (BL-419, PR #520); added markers smoke tests (PR #521); added `sample_rate`/`bit_depth` project audio baseline with Pydantic Literal validation, PRAGMA-idempotent Alembic migration, and `RenderSettings` Rust `with_audio()` builder (BL-422, PR #522); added audio baseline smoke tests (PR #523). See [CHANGELOG.md](docs/CHANGELOG.md) for release details.

# SonarCloud configuration for stoat-and-ferret
# Project key and organization: retrieve from SonarCloud UI if not already set.
sonar.projectKey=gwickman_stoat-and-ferret
sonar.organization=gwickman

# Exclude render-verification scripts from copy-paste detection only.
# These scripts deliberately duplicate _validate_host to preserve stdlib-only
# autonomy (BL-641 design decision, accepted duplication tradeoff).
# Do NOT "fix" this by extracting a shared module — that would break the
# stdlib-only self-contained script design. (BL-693)
sonar.cpd.exclusions=scripts/verify_render_output.py,scripts/examples/wait-for-render.py,scripts/examples/dump-ws-events.py
